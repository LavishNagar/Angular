import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrl: './app.component.css'
})
export class AppComponent {
  // title = 'blog-app';
  // obj: { name: string, id: number } = { name: 'lavish', id: 5364 };
  // date=Date();

  // note=5343534;

// create custom pipe

}
